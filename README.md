# mongoDB-NodeJsConnection
CRUD operations -  MongoDB and NodeJS

# check node version
  node -v
  
# install mongodb driver
  npm install mongodb

# check mongoDB version
  npm list mongodb
  
 *(In the same folder where index.js is present)
